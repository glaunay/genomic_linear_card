export declare function nbWord(sequences: Object): number;
export declare class TreeClustering {
    root: Object;
    constructor(sizeGenome: number, sequences: any, nbSec: number, minWord: number);
    find(min: any, max: any, sequences: any): Object;
    constructTree(sizeSector: number, sequences: Object, nbSec: number, minWord: number, niv: any, minPos: number): any[];
}
